GESAMT-EMPFEHLUNG: GO mit Korrekturen

Festlegung 1 (Schatten-Quartier):    NACHBESSERN — Als Prinzip deutlich kleiner als ein NULL-Refactor, weil `households.quarter_id` und `get_user_quarter_id()` die bestehende Quartier-RLS weiter tragen koennen (`051_multi_quarter_foundation.sql`, `052_quarter_rls_policies.sql`). Aber `5 Stellen` ist zu optimistisch: Registrierung, Haushaltsanlage, QuarterProvider/Quarter-Hooks, direkte `currentQuarter`-Queries und UI-Maskierung haengen heute sichtbar an echtem Quartier (`app/(auth)/register/components/RegisterStepAddress.tsx`, `RegisterStepIdentity.tsx`, `lib/services/registration.service.ts`, `lib/services/household.service.ts`, viele `app/(app)/*`-Routen).
Festlegung 2 (civic_orgs+housing):    NACHBESSERN — `civic_organizations` hat bereits eine freie `type`-Spalte; `type='housing'` ist strukturell zulaessig und Migration-seitig kein harter Blocker (`114_civic_portal.sql`). Die civic-Bausteine `civic_messages`, `civic_message_attachments`, `civic_announcements` und ihre RLS laufen org-id-basiert und waeren type-agnostisch; aber `municipal_reports`, `municipal_announcements`, `/org`, `/api/organizations`, Nav und Teile von Billing/Push haengen weiter an `organizations/org_members`, nicht an `civic_organizations/civic_members`.
Festlegung 3 (report_category+target): NACHBESSERN — `municipal_reports` ist heute rein quartiergebunden und hat noch keinen Org-Pointer (`097_municipal_reports.sql`), also ist `target_org_id` als Erweiterung technisch plausibel. Die neue Housing-Taxonomie kollidiert aber nicht nur im Enum, sondern auch in App-Typen, Konstanten und Formularen (`lib/municipal/types.ts`, `lib/municipal/constants.ts`, `app/(app)/reports/new/page.tsx`, `app/(app)/org/reports/OrgReportsClient.tsx`); ausserdem muessen die Report- und Kommentar-Policies target-aware werden.
Festlegung 4 (housing_appointments):  NACHBESSERN — Eine neue Mini-Tabelle ist nachvollziehbar, weil `civic_appointments` keinen Org-/Haushaltsbezug hat und `care_appointments` fachlich aus dem Pflege-Kontext kommt (`114_civic_portal.sql`, `026_care_appointments.sql`). Das vorgeschlagene Minimal-Schema ist aber zu knapp fuer saubere Repo-Integration: mindestens `created_by`, `updated_at` und ein Freitext-/Notizfeld fehlen; fuer Reminder-/UI-Reuse waere ausserdem die Abweichung von den bestehenden Terminmustern (`scheduled_at` + `duration_minutes`) zu bedenken (`065_consultation_slots.sql`, `app/(app)/care/termine/page.tsx`).
Festlegung 5 (Validation-Tor):        OK (nicht zu pruefen, Founder-Aktion)
Cockpit (a) Adaption org/:            NACHBESSERN — Als UI-Huelle ist `app/(app)/org/` die kleinste Variante: vorhandenes Layout, Tabs und Seitenstruktur sind schlank und erweiterbar (`app/(app)/org/layout.tsx`, `page.tsx`, `announcements/`, `reports/`). Die versteckte Falle ist die Daten-/Rollenverdrahtung: `/org`-Navigation, API und Rollen-Erkennung laufen heute ueber `organizations/org_members`, nicht ueber `civic_members` (`components/nav/NavConfig.ts`, `app/api/organizations/route.ts`, `modules/admin/services/organizations.service.ts`).
Querschnitt (Link/Push/Kachel):       NACHBESSERN — Ein separater Resident-Link ist deutlich sauberer als `org_members role=resident`, weil `org_members` und Nav-/Org-Logik sonst Bewohner in Staff-UI hineinziehen wuerden (`components/nav/NavConfig.ts`, `073_organizations.sql`, `132_pflege_resident_assignments.sql`, `071_caregiver_links.sql`). Push ist fuer Bewohner grundsaetzlich reuse-faehig (`notifications`, `/api/push/notify`), aber der bestehende Org-Staff-Helper zeigt gerade auf `organizations/org_members` statt auf civic-Org-Mitglieder (`lib/push-delivery.ts`, `app/api/postfach/route.ts`); die Bewohner- und Senior-Kacheln sind ausserdem an mehreren festen Stellen hart verdrahtet (`components/dashboard/QuickActions.tsx`, `components/dashboard/DiscoverGrid.tsx`, `app/(senior)/kreis-start/page.tsx`, `app/senior/home/page.tsx`).

Konkrete Korrekturen (falls "NACHBESSERN"):
1. Festlegung 1 — Schatten-Quartier nicht nur als Quarter-Seed denken, sondern als vollstaendigen Registrierungs-/Haushalts-Pfad — warum: der aktuelle Register-Flow hat keinen echten "ohne Quartier"-Skip, und `completeRegistration()` haengt die Quartier-Zuordnung heute an die Haushaltsanlage (`RegisterStepAddress.tsx`, `RegisterStepIdentity.tsx`, `lib/services/registration.service.ts`, `lib/services/household.service.ts`).
2. Festlegung 1 — `5 Stellen` auf `mehrere zusammenhaengende Bereiche` korrigieren — warum: neben Registration/RLS haengen auch QuarterProvider und direkte Frontend-Queries an `currentQuarter`, z. B. Dashboard, Reports, Board, Help, Marketplace, News (`052_quarter_rls_policies.sql`, `app/(app)/dashboard/hooks/useDashboardData.ts`, diverse `app/(app)/*`-Seiten mit `.eq("quarter_id", currentQuarter.id)`).
3. Festlegung 2 — `civic_organizations.type='housing'` beibehalten, aber die Aussage "Type-Add, kein Tabellen-Refactor" abschwaechen — warum: civic-seitig stimmt das fuer Postfach/Bekanntmachungen, nicht aber fuer die heute sichtbaren Org-/Municipal-Pfade, die weiterhin `organizations/org_members` verwenden (`114_civic_portal.sql`, `146_civic_messages.sql`, `149_civic_message_attachments.sql`, `097_municipal_reports.sql`, `098_municipal_announcements.sql`, `app/api/organizations/route.ts`).
4. Festlegung 3 — `target_org_id` nur zusammen mit einer Anpassung von Enum, App-Typen, Formularen und Kommentar-/Select-Policies planen — warum: die alten 6 Report-Kategorien sind sowohl in SQL als auch in TS/UI hart hinterlegt; ohne App-Seite und Kommentar-RLS laeuft die Migration nur halb (`097_municipal_reports.sql`, `lib/municipal/types.ts`, `lib/municipal/constants.ts`, `app/(app)/reports/new/page.tsx`).
5. Festlegung 3 — Fuer Staff-Zugriff bei Housing-Reports das civic-Org-Muster als Vorbild nehmen, nicht die heutige `org_members`-Moderation — warum: `civic_messages`/`citizen_reports` zeigen bereits org-id-basierte Staff-Policies; die aktuelle Municipal-Moderation haengt noch am Quartier-Admin der anderen Org-Welt (`146_civic_messages.sql`, `114_civic_portal.sql`, `097_municipal_reports.sql`).
6. Festlegung 4 — `housing_appointments` um `created_by`, `updated_at` und ein Textfeld ergaenzen; Reminder nicht implizit vergessen — warum: die bestehenden Terminmuster im Repo arbeiten mit Verantwortlichem, Notizen und Aktualisierungszeitpunkt, und die Produktbeschreibung nennt explizit Erinnerungen (`026_care_appointments.sql`, `065_consultation_slots.sql`, `app/(app)/care/termine/page.tsx`).
7. Querschnitt — Resident-Verknuepfung als eigene Assignment-/Link-Tabelle halten, nicht als neue Org-Mitgliedsrolle — warum: `org_members`/`civic_members` sind Staff-Mitgliedschaften; eine `resident`-Rolle wuerde Navigation, Guards und Orga-Views seitlich kaputt machen (`components/nav/NavConfig.ts`, `073_organizations.sql`, `114_civic_portal.sql`, `132_pflege_resident_assignments.sql`).
8. Querschnitt — Fuer Push und Staff-Benachrichtigungen die bestehende Pipeline nur nach Korrektur des Org-Lookups als reuse-faehig markieren — warum: `notifyOrgStaff()` sucht aktuell in `organizations/org_members`, waehrend das Postfach auf `civic_organizations` basiert (`lib/push-delivery.ts`, `app/api/postfach/route.ts`).
9. Querschnitt — Die HV-Kachel nicht als einen zentralen Config-Schalter unterschaetzen — warum: normale App und Senior-Ansichten haben mehrere feste Tile-Definitionen, nicht eine gemeinsame Registry (`components/dashboard/QuickActions.tsx`, `components/dashboard/DiscoverGrid.tsx`, `app/(senior)/kreis-start/page.tsx`, `app/senior/home/page.tsx`).

Showstopper (falls vorhanden):
- Keine neuen Showstopper in den 5 Festlegungen gefunden. Die groessten Probleme sind echte Korrekturpunkte, keine strukturellen Blockaden.

Kleinere Risiken/Hinweise (max. 5):
- `OnboardingFlow.tsx` referenziert `quarter_memberships`, das ich sonst im Repo nicht finde. Das ist kein HV-Showstopper, aber ein Hinweis darauf, dass der bestehende Onboarding-/Quartierpfad bereits Drift hat (`modules/onboarding/components/OnboardingFlow.tsx`).
- `civic_appointments` existiert doch bereits im Repo, aber ohne Org-/Haushaltsbindung und mit sehr breiter RLS (`114_civic_portal.sql`). Die Existenz darf nicht mit Eignung fuer Housing verwechselt werden.
- `notifications` akzeptiert heute keine housing-spezifischen Typen per Constraint; Fallback auf `system` existiert, aber fuer saubere UX sollte entweder ein vorhandener Typ wiederverwendet oder der Check erweitert werden (`037_fix_notification_types_and_cron.sql`, `lib/notifications-server.ts`, `app/(app)/notifications/page.tsx`).
- Die Push-Portale kennen heute `io | civic | arzt | pflege`, kein `housing` (`lib/push-delivery.ts`). Das ist kein eigener Kanal-Showstopper, solange Bewohner auf `io` und HV-Staff auf einem korrigierten civic-Pfad bleiben.
- Es gibt zwei Senior-Startwelten mit festen Aktionen: `/kreis-start` und `/senior/home`. Wenn Housing nur in eine davon eingehangen wird, entsteht sofort Produkt-Drift (`app/(senior)/kreis-start/page.tsx`, `app/senior/home/page.tsx`).
