GESAMT-EMPFEHLUNG: NO-GO

Pre-Check:        SHOWSTOPPER — Die Pre-Check-Tabelle ist gravierend zu optimistisch. Bereits vorhanden sind `municipal_reports` + Resident-UI + Org-Moderation (`supabase/migrations/097_municipal_reports.sql`, `app/(app)/reports/*`, `app/(app)/org/reports/*`), `municipal_announcements` + Org-Redaktion (`supabase/migrations/098_municipal_announcements.sql`, `app/(app)/org/announcements/page.tsx`), ein Postfach mit Attachments/Lesestatus (`supabase/migrations/146_civic_messages.sql`, `149_civic_message_attachments.sql`, `app/api/postfach/*`) und `housing` ist im Org-Code bereits verdrahtet (`app/(app)/org/page.tsx`, `components/org/OrgDashboard.tsx`, `lib/organizations.ts`).
Pivot-These:      NACHBESSERN — Der Pivot "Senior-Mieter-Kanal als Add-on" ist technisch plausibel und kollidiert nicht sichtbar mit Repo-Regeln. Er ist aber nur tragfaehig, wenn er strikt Kommunikations-/Servicekanal bleibt; sonst laeuft er strategisch wieder in die eingefrorene Portal-Breite aus `memory/topics/strategy-phase1.md`.
Funktionsumfang:  NACHBESSERN — Die 4 Funktionen sind nicht 4 Greenfield-Features, sondern eher 4 Adapter auf vorhandene Bausteine. Besonders "Dokumenten-Postfach mit Vorlesen" ist nicht 1:1 reuse-faehig, weil `tts-cache` explizit keine user-spezifischen/sensiblen Texte cachen darf (`supabase/migrations/168_tts_phrase_cache_bucket.sql`), und "Maengel-Diktat" ist im Voice-Stack nicht plug-and-play, weil dort auf `issue_reports` gezeigt wird, das ich sonst im Repo nicht finde (`modules/voice/services/tool-executor.ts`).
Architektur:      NACHBESSERN — Zwei Layouts sind mit bestehendem App-Router sauber loesbar; das Repo nutzt bereits mehrere Route-Gruppen und Sub-Layouts (`app/(app)/layout.tsx`, `app/(senior)/layout.tsx`, `app/(app)/org/layout.tsx`). Das Risiko liegt nicht im Next.js-Layout, sondern darin, parallel zu `org` und zum Civic-Stack ein drittes Cockpit/Data-Model aufzubauen.
RLS:              NACHBESSERN — Eine neue globale Rolle `housing_admin` wirkt nach aktuellem Stand nicht noetig. Das vorhandene Modell `organizations` + `org_members(role=admin/viewer)` + `org_admin/org_viewer`-Feature-Gates deckt die Grundlogik schon ab; neu gebraucht wird eher die korrekte Bindung von Org zu Haus/Haushalt/Wohnung als noch ein weiteres Rollen-System.
Billing:          NACHBESSERN — Die bestehende Billing-Infra ist fuer dieses B2B-Housing-Modell nicht "technisch sauber" wiederverwendbar. Phase-1-Checkout erlaubt aktuell nur `plus` (`lib/services/billing-checkout.service.ts`), `care_subscriptions` ist historisch inkonsistent dokumentiert (`supabase/migrations/060_subscription_plans_v2.sql`, `memory/topics/billing.md`), und die vorhandene Pro-Community-Provisionierung ist fuer Org-Billing offensichtlich nicht fertig genug.
Abgrenzung Hilfe: OK — `hilfe` bleibt fachlich Nachbar-zu-Nachbar: `help_requests`, Matching, Sessions, Receipts und Helfer-Billing (`app/api/hilfe/*`, `modules/hilfe/services/types.ts`). Es gibt keine direkte Code-Kollision mit Mieter-zu-Hausverwaltung; hoechstens einzelne UI-/Upload-/Reminder-Bausteine sind wiederverwendbar.

Konkrete Korrekturen (falls "NACHBESSERN"):
1. Pre-Check — Vor jeder weiteren Architekturentscheidung muessen `municipal_reports`, `municipal_announcements`, `civic_messages`/Attachments, `civic_appointments`, `org`-Bereich und bestehende Billing-/Org-Pfade als vorhandene Basis in den Vorschlag aufgenommen werden — sonst wird der Plan an mehreren Stellen Duplikat.
2. Pivot-These — Die Positionierung muss explizit auf "Senior-Mieter-Kommunikationskanal fuer bestehende HV-Software" verengt bleiben — warum: `memory/topics/strategy-phase1.md` warnt klar vor neuer Portal-Breite.
3. Funktionsumfang — "Brief vorlesen" darf nicht pauschal auf den vorhandenen Public-TTS-Cache gestuetzt werden, sondern braucht einen sensiblen Privatpfad ohne Cache-Missbrauch — warum: `168_tts_phrase_cache_bucket.sql` verbietet genau diese Art Wiederverwendung.
4. Architektur — Kein neues Greenfield-Cockpit planen, bevor entschieden ist, was auf `organizations/org_members/org/*` aufsetzt und was aus dem Civic-Postfach wirklich adapterbar ist — warum: das Repo hat heute schon zwei Org-Welten (`organizations` und `civic_organizations`).
5. RLS — Kein neues globales Rollenpaar `housing_admin`/`housing_resident_link` als Default annehmen; zuerst die Haus-/Haushalts-Zuordnung modellieren und nur dann Rollen erweitern, wenn `admin/viewer` fachlich nicht reichen — warum: der aktuelle Rollenstapel ist bereits mehrschichtig genug.
6. Billing — `organizations.stripe_customer_id` nicht als "fertig nutzbar" behaupten; zuerst die reale Checkout-/Webhook-/Provisioning-Luecke als offen markieren — warum: der aktuelle Billing-Stand ist laut Repo selbst noch inkonsistent und auf `plus` fokussiert.

Showstopper (falls vorhanden):
- Pre-Check gravierend falsch — vorhandene Meldungs-, Bekanntmachungs-, Postfach-, Termin- und Org-Infrastruktur wurde uebersehen; damit ist "neu bauen" in der vorgeschlagenen Form nicht reviewbar — belegt durch `097_municipal_reports.sql`, `098_municipal_announcements.sql`, `114_civic_portal.sql`, `146_civic_messages.sql`, `149_civic_message_attachments.sql`, `app/(app)/org/*`, `app/(app)/reports/*`, `app/api/postfach/*`.

Risiken, die Claude nicht erwaehnt hat:
- Das Repo hat bereits zwei Organisationsmodelle (`organizations` und `civic_organizations`). Wenn das ignoriert wird, entsteht sehr schnell eine dritte, widerspruechliche HV-Variante.
- Die vorhandenen kommunalen Tabellen sind quartierskopiert; Hausverwaltung braucht typischerweise Haus-/Haushalts-/Wohnungs-Scoping. `households` hat inzwischen Adressmetadaten (`156_household_position_metadata.sql`), aber noch keine saubere Org-zu-Haus-Bindung.
- Der vorhandene TTS-Cache ist fuer nicht-sensitive Standardphrasen gedacht, nicht fuer private Mieterpost. Das ist kein Detail, sondern ein Datenschutz-/Architekturrisiko.
- Die Voice-Pipeline suggeriert bereits "Maengelmeldung", zeigt aber im Tool-Executor auf `issue_reports`, das ich sonst im Repo nicht finde. Fuer Sprach-Diktat ist also mehr Integrationsarbeit noetig, als der Brief vermuten laesst.
- Das bestehende Org-/Billing-Provisioning wirkt fuer B2B nicht fertig genug; insbesondere ist die Wiederverwendung der aktuellen Stripe-/Subscription-Pfade fuer zahlende Hausverwaltungen ohne Vorarbeit riskant.
